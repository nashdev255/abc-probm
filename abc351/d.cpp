#include <bits/stdc++.h>
#define rep(i,n) for(int i=0;i<n;++i)
using namespace std;
using ll = long long;

// 潔く諦めます

int main() {
  int H,W;
  int ans,ansa,ansb;
  cin>>H>>W;
  vector<string> S(H);
  for(auto& s:S)cin>>s;
  return 0;
}
